1. "Extract all tabular data from the PNG image. Convert it to CSV format, preserving the exact row and column structure. Do not interpret or infer data—transcribe precisely. Use commas to separate fields and keep headers intact as shown."
- aiming for consistent output / layout

2. "Use OCR to extract text from the PNG. Identify any tables, then convert them into structured CSV format with rows and columns matching the image. Retain header names and numerical formatting. Output only CSV data."
- Forces a consistent structure

3. "Detect all headers and their respective columns in the image. Build a CSV where each column header corresponds to a header in the image, and each row to an entry below that header. Ignore non-tabular text."
- to minimize rows skipped

4. "Analyze the visual structure of the PNG. Use horizontal and vertical alignment of text to detect grid cells. Convert this spatial grid into a CSV with equivalent rows and columns. Do not deduce values—only extract."
- Best for fixed-grid data

5. “Read this PNG table and convert all entries into a CSV file. Include headers and preserve the original table structure exactly. Ignore annotations, footnotes, or graphical embellishments.”
- digitizing clear tables; replicates structure.

6. “Perform OCR on this PNG image to extract dates and case numbers. Infer the column structure based on horizontal alignment and output a clean CSV. Trim any whitespace or garbled text.”
- Adaptable to various layouts; good for scanned documents.

7. "Use OCR to extract tabular data from this PNG image and format it into CSV, ensuring all rows and columns from the image are preserved. Maintain original data alignment and structure."
- good for handwritten or scanned documents?

8. "Identify the data grid in this PNG image and reconstruct it as a CSV file. Focus on preserving the number of rows and columns, with each cell's value placed exactly as it appears in the table."
- focus on geometric structure; good for grid-based datasets.

9. "Extract data from this PNG table row-by-row into CSV format. Start from the topmost data row and move downward, treating each horizontal segment as a new row in the output."
- Ensures no rows are skipped

10. "Parse this PNG image column-by-column and transcribe the data into CSV format. Retain the order of columns as seen from left to right in the image."
- Good when OCR detects columns more accurately than rows



