* How to write prompts?
    - Use the LLM to create the prompt
    - Maybe make longer prompt
* Inference providers are important : e.g. `together.ai`
* Open AI fine tuning service -- probably aws `textract`
* In-context learning (model weights do not change) vs fine tuning (they do)
